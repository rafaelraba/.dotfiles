#!/usr/bin/env bash
set -euo pipefail

# DEPRECATED: This AeroSpace layout helper is no longer used.
# The active config applies layouts directly in Hammerspoon via hs.window.
# Kept for reference only. It does nothing and exits safely.

echo "aerospace-dev-layout.sh is deprecated and no longer executes any AeroSpace layout commands." >&2
exit 0
